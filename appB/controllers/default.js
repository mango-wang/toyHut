'use strict';
define([
    './controllers'], function (controllers) {
    controllers.
    controller('default', [
        '$rootScope',
        '$scope',
        '$route',
        '$timeout',
        function ($rootScope, $scope, $route,  $timeout) {



        }])
});
